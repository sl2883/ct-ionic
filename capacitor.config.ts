import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sunny.ctios',
  appName: 'CapApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
